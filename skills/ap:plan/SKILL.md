---
description: "Analyze requirements and create feature/design tasks (Planner-only)"
argument-hint: "[requirement]"
---

# /ap:plan

用户执行了 `/ap:plan $ARGUMENTS`。

请加载 `agent-protocol` skill 并以 Planner 角色分析需求或架构，创建 feature/design task 并追加到 `.agent-memory/tasks.json`。
