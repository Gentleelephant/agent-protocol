---
description: "List tasks by status (read-only, any role)"
argument-hint: "[status]"
---

# /ap:tasks

用户执行了 `/ap:tasks $ARGUMENTS`。

请加载 `agent-protocol` skill 并列出任务。通用只读命令，任何角色均可执行。
省略 status 时列出所有任务。
