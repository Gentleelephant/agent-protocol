---
description: "Show configured Planner and Executor for this project (read-only, any role)"
---

# /ap:whoami

用户执行了 `/ap:whoami`。

请加载 `agent-protocol` skill 并显示当前项目配置的 Planner 和 Executor。通用只读命令，任何角色均可执行。
