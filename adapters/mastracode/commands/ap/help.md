---
name: ap:help
description: "Show command help. Read-only, any role."
argument-hint: "[command]"
---

# /ap:help

显示命令帮助。通用只读命令，任何角色均可执行。省略 command 时列出所有命令及摘要。

支持的命令：`init`、`review`、`plan`、`task`、`verify`、`execute`、`fix`、`test`、`done`、`tasks`、`status`、`help`、`whoami`。
