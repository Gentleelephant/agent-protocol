---
description: "Show command help (read-only, any role)"
argument-hint: "[command]"
---

# /ap:help

用户执行了 `/ap:help $ARGUMENTS`。

请加载 `agent-protocol` skill 并显示命令帮助。通用只读命令，任何角色均可执行。
省略 command 时列出所有命令及摘要。
