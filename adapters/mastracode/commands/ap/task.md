---
description: "Save discussion result as a pending task (Planner-only)"
argument-hint: "[summary]"
---

# /ap:task

用户执行了 `/ap:task $ARGUMENTS`。

请加载 `agent-protocol` skill 并以 Planner 角色将当前讨论结果保存为 pending task，追加到 `.agent-memory/tasks.json`。
