---
description: "Run verification and record results (Executor-only)"
argument-hint: "[task-id]"
---

# /ap:test

用户执行了 `/ap:test $ARGUMENTS`。

请加载 `agent-protocol` skill 并以 Executor 角色运行验证并记录结果。
省略参数时针对当前 in_progress task。
