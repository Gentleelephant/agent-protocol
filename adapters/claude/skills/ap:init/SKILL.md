---
description: "Initialize or update project-level personal protocol config"
argument-hint: "planner=<agent> executor=<agent>"
---

# /ap:init

用户执行了 `/ap:init $ARGUMENTS`。

请加载 `agent-protocol` skill 并按照 Init Workflow 执行项目初始化或配置更新。
这是一个配置命令，任何 agent 均可执行，不受角色门禁限制。
